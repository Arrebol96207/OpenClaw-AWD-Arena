# Git 提交计划

## 任务
将所有优化改动提交到 git。

## 改动概览
- 65 个文件修改，11152 行新增，4793 行删除
- 包含安全修复、代码质量改善、性能优化、Docker 镜像优化

## 提交步骤

1. **暂存所有改动**
   ```bash
   git add -A
   ```

2. **提交**
   ```bash
   git commit -m "refactor: 安全修复、代码质量改善与性能优化

- 安全: 轮换泄露的 API Key，升级 aiohttp/python-multipart/fastapi 修复 CVE
- 安全: 限制 CORS 配置为具体 HTTP 方法和请求头
- 代码: 创建 env_utils.py/docker_utils.py 消除重复代码
- 代码: 改善 main.py 错误处理，细化 Docker 异常捕获
- 性能: WebSocket 并行广播(asyncio.gather) + 心跳清理死连接
- 性能: 数据库连接复用，前端 AGENT_STREAM 300ms 防抖
- Docker: nginx 锁定 1.27-alpine，referee 多阶段构建 + Python 3.12"
   ```

3. **验证**
   ```bash
   git status
   git log -1
   ```

## 注意事项
- `.env` 文件已被 `.gitignore` 排除，不会被提交
- `.mimocode/` 目录是临时文件，不应提交

---

## 阶段一：安全修复（P0 - 必须立即处理）

### 1.1 轮换泄露的 API Key

**问题**: `.env` 文件中包含真实的 DeepSeek API Key  
**文件**: `.env` (第 6-7 行, 第 14-15 行)

**操作步骤**:
1. 立即在 DeepSeek 控制台轮换 API Key
2. 更新 `.env` 文件使用新 Key
3. 确认 `.gitignore` 已排除 `.env`
4. 使用 `git filter-branch` 或 BFG 从 Git 历史中清除旧 Key

### 1.2 升级有漏洞的依赖包

**文件**: `referee-engine/requirements.txt`

| 包 | 当前版本 | 目标版本 | CVE |
|---|---|---|---|
| `aiohttp` | 3.9.3 | 3.10.0+ | CVE-2024-23334, CVE-2024-23829 |
| `python-multipart` | 0.0.6 | 0.0.12+ | CVE-2024-53982 |
| `fastapi` | 0.109.0 | 0.115.0+ | 安全更新 |

**操作步骤**:
```bash
cd referee-engine
# 更新 requirements.txt
pip install --upgrade aiohttp python-multipart fastapi
pip freeze > requirements.txt
# 运行测试验证兼容性
pytest tests/
```

### 1.3 加强 CORS 配置

**文件**: `referee-engine/main.py` (第 3751-3753 行)

**当前问题**: 允许所有 HTTP 方法和请求头  
**建议**: 限制为实际使用的方法和头部

---

## 阶段二：代码质量改善（P1 - 高优先级）

### 2.1 拆分 main.py（4269 行）

**目标文件**: `referee-engine/main.py`

**拆分方案**:

```
referee-engine/
├── main.py                    # FastAPI 应用入口 + 路由注册（~500 行）
├── match_engine.py            # MatchState + RefereeEngine 核心逻辑（~1500 行）
├── container_orchestrator.py  # Docker 容器创建/销毁/网络管理（~500 行）
├── websocket_manager.py       # WebSocket 连接管理 + 广播（~300 行）
├── match_routes.py            # 比赛相关 API 路由（~800 行）
├── werewolf_routes.py         # 狼人杀相关 API 路由（~400 行）
├── template_routes.py         # 模板相关 API 路由（~200 行）
└── admin_routes.py            # 管理相关 API 路由（~200 行）
```

**拆分步骤**:
1. 创建新文件结构
2. 提取 `MatchState` 类到 `match_engine.py`
3. 提取 `RefereeEngine` 类到 `match_engine.py`
4. 提取 Docker 相关方法到 `container_orchestrator.py`
5. 提取 WebSocket 管理到 `websocket_manager.py`
6. 按功能分组路由到各 `*_routes.py`
7. 在 `main.py` 中导入并注册路由
8. 运行完整测试套件验证

### 2.2 消除代码重复

**2.2.1 统一 `_docker_exec` 函数**

**重复位置**:
- `referee-engine/main.py` (第 939-977 行)
- `referee-engine/flag_manager.py` (第 36-55 行)

**方案**: 创建 `referee-engine/docker_utils.py`，提取共享实现

**2.2.2 统一 stream callback 函数**

**重复位置**:
- `_make_agent_stream_callback` (main.py:2262-2289)
- `_make_werewolf_agent_stream_callback` (main.py:1829-1851)
- `_match_timer` 内的 `make_stream_cb` (main.py:2764-2791)

**方案**: 创建统一的 `create_stream_callback(phase, broadcast=True)` 工厂函数

**2.2.3 统一 keepalive 发送逻辑**

**重复位置**:
- `_send_defense_keepalive` (main.py:2291-2346)
- `_send_attack_keepalive` (main.py:2406-2483)

**方案**: 提取 `_send_keepalive(phase, prompt_template, message_kind)` 方法

**2.2.4 统一环境变量解析辅助函数**

**重复位置**:
- `werewolf.py` (第 134-143 行)
- `commentator.py` (第 97-106 行)

**方案**: 创建 `referee-engine/env_utils.py`

### 2.3 改善错误处理

**2.3.1 细化异常捕获**

**文件**: `referee-engine/main.py` (第 1643-1653 行)

**当前问题**: 捕获所有 `Exception`，不区分可恢复/不可恢复错误  
**方案**:
```python
except docker.errors.ImageNotFound as e:
    logger.error(f"Image not found: {e}")
    match.status = "error"
    match.error_detail = "missing_image"
except docker.errors.APIError as e:
    logger.error(f"Docker API error: {e}")
    match.status = "error"
    match.error_detail = "docker_api_error"
except Exception as e:
    logger.exception(f"Unexpected error: {e}")
    match.status = "error"
    match.error_detail = "unknown"
```

**2.3.2 添加数据库事务保护**

**文件**: `referee-engine/database.py` (第 632-642 行)

**方案**:
```python
def _delete_match_sync(match_id: str) -> int:
    conn = _connect()
    try:
        conn.execute("BEGIN TRANSACTION")
        conn.execute("DELETE FROM events WHERE match_id = ?", (match_id,))
        conn.execute("DELETE FROM submissions WHERE match_id = ?", (match_id,))
        cur = conn.execute("DELETE FROM matches WHERE match_id = ?", (match_id,))
        conn.commit()
        return cur.rowcount
    except Exception:
        conn.rollback()
        raise
    finally:
        conn.close()
```

### 2.4 改善类型安全

**2.4.1 减少 `Any` 使用**

**文件**: `referee-engine/main.py`

**方案**:
- 为 `player_clients` 定义具体类型：`Dict[int, AgentBackendAdapter]`
- 为 Docker 操作返回值定义 Protocol
- 使用 `TypedDict` 替代 `Dict[str, Any]`

**2.4.2 改善 `getattr` 使用**

**文件**: `referee-engine/player_tokens.py` (第 12-14 行)

**方案**: 使用 Protocol 或具体类型替代 `object` + `getattr`

### 2.5 补充测试

**2.5.1 为 main.py 添加单元测试**

**新建文件**: `referee-engine/tests/unit/test_main.py`

**测试范围**:
- `MatchState` 类的核心方法
- `RefereeEngine` 的比赛生命周期管理
- API 路由的请求/响应验证

**2.5.2 为前端添加组件测试**

**新建文件**: `frontend/tests/unit/ArenaPage.test.tsx`

**测试范围**:
- `buildWerewolfState` 纯函数
- `buildWerewolfObservations` 纯函数
- WebSocket 消息处理逻辑

**2.5.3 为数据库层添加测试**

**新建文件**: `referee-engine/tests/unit/test_database.py`

**测试范围**:
- CRUD 操作
- 事务完整性
- 并发访问

---

## 阶段三：性能优化（P2 - 中优先级）

### 3.1 WebSocket 广播优化

**文件**: `referee-engine/main.py` (第 3452-3471 行)

**当前问题**: 串行发送给每个客户端  
**方案**: 改为并行发送

```python
async def broadcast(self, message: dict):
    safe_message = sanitize_public_payload(message)
    if not isinstance(safe_message, dict):
        safe_message = {}
    msg_match_id = safe_message.get("match_id")
    
    targets = [
        ws for ws in self.ws_connections
        if not (msg_match_id and self.ws_subscriptions.get(ws) 
                and self.ws_subscriptions.get(ws) != msg_match_id)
    ]
    
    async def _send(ws):
        try:
            await ws.send_json(safe_message)
            return ws, True
        except Exception:
            return ws, False
    
    results = await asyncio.gather(
        *[_send(ws) for ws in targets], 
        return_exceptions=True
    )
    for ws, ok in results:
        if not ok:
            self.ws_connections.discard(ws)
            self.ws_subscriptions.pop(ws, None)
```

### 3.2 添加 WebSocket 心跳机制

**文件**: `referee-engine/main.py`

**方案**: 添加定期 ping/pong 清理死连接

```python
async def _ws_heartbeat_loop(self):
    while True:
        await asyncio.sleep(30)
        dead = []
        for ws in list(self.ws_connections):
            try:
                await ws.send_json({"type": "ping"})
            except Exception:
                dead.append(ws)
        for ws in dead:
            self.ws_connections.discard(ws)
            self.ws_subscriptions.pop(ws, None)
```

### 3.3 数据库连接复用

**文件**: `referee-engine/database.py`

**当前问题**: 每次操作创建新连接  
**方案**: 使用进程级共享连接

```python
_global_conn: Optional[sqlite3.Connection] = None

def _get_connection() -> sqlite3.Connection:
    global _global_conn
    if _global_conn is None:
        _global_conn = _connect()
    return _global_conn
```

### 3.4 消除 N+1 查询

**文件**: `referee-engine/database.py`

**问题**: `_load_all_matches_sync()` 对每个 match 单独查 events  
**方案**: 使用 JOIN 批量查询

```sql
SELECT m.*, e.event_type, e.data_json, e.timestamp
FROM matches m 
LEFT JOIN events e ON m.match_id = e.match_id
ORDER BY m.created_at DESC, e.timestamp ASC
```

### 3.5 前端渲染优化

**3.5.1 vis-network 动态加载**

**文件**: `frontend/src/components/TopologyMap.tsx`

**方案**: 使用 `React.lazy()` 延迟加载 500KB 的 vis-network

```tsx
const TopologyMapCanvas = React.lazy(() => import('./TopologyMapCanvas'))
```

**3.5.2 WebSocket 消息批量缓冲**

**文件**: `frontend/src/pages/ArenaPage.tsx`

**方案**: 对高频 `AGENT_STREAM` 事件做批量缓冲

```tsx
const streamBuffer = useRef<string[]>([])
useEffect(() => {
  const timer = setInterval(() => {
    if (streamBuffer.current.length > 0) {
      setStreamLines(prev => [...prev, ...streamBuffer.current.splice(0)])
    }
  }, 100)
  return () => clearInterval(timer)
}, [])
```

**3.5.3 拆分 ArenaPage 组件**

**文件**: `frontend/src/pages/ArenaPage.tsx` (1636 行)

**方案**: 拆分为独立子组件
- `ArenaPageAWD.tsx` - AWD 模式视图
- `ArenaPageWerewolf.tsx` - 狼人杀模式视图
- 使用 `React.memo` 减少不必要的渲染

---

## 阶段四：Docker 镜像优化（P3 - 低优先级）

### 4.1 锁定镜像版本

**文件**: `frontend/Dockerfile`, `referee-engine/Dockerfile`

**当前问题**: `nginx:alpine` 和 `alpine/openclaw:latest` 未锁定版本  
**方案**:
```dockerfile
# frontend/Dockerfile
FROM nginx:1.27-alpine

# agent-image/Dockerfile.local
FROM alpine/openclaw:v1.2.3  # 使用具体版本
```

### 4.2 Referee Dockerfile 多阶段构建

**文件**: `referee-engine/Dockerfile`

**方案**:
```dockerfile
FROM python:3.12-slim AS builder
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir --prefix=/install -r requirements.txt

FROM python:3.12-slim
WORKDIR /app
COPY --from=builder /install /usr/local
RUN apt-get update && apt-get install -y --no-install-recommends \
    curl openssh-client tzdata docker-cli \
    && rm -rf /var/lib/apt/lists/*
ENV TZ=Asia/Shanghai
COPY . .
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 4.3 升级 Python 版本

**当前**: Python 3.9（2025-10 EOL）  
**目标**: Python 3.12（性能提升 + 长期支持）

---

## 验证方案

### 安全修复验证
```bash
# 1. 检查 .env 不再包含真实 Key
grep -r "sk-" .env

# 2. 运行依赖安全扫描
pip install pip-audit
pip-audit -r requirements.txt

# 3. 运行测试套件
cd referee-engine && pytest tests/
```

### 代码质量验证
```bash
# 1. 运行类型检查
cd referee-engine && mypy --ignore-missing-imports .

# 2. 运行代码风格检查
cd referee-engine && ruff check .

# 3. 运行完整测试套件
cd referee-engine && pytest tests/ -v

# 4. 前端构建验证
cd frontend && npm run build
```

### 性能优化验证
```bash
# 1. 前端打包大小检查
cd frontend && npm run build && ls -lh dist/assets/

# 2. WebSocket 并发测试
# 使用 wscat 或自定义脚本测试多客户端并发广播

# 3. 数据库查询性能
# 对比优化前后的查询时间
```

---

## 预期收益

| 维度 | 当前状态 | 优化后 |
|---|---|---|
| 安全性 | 有漏洞依赖 + 泄露 Key | 所有依赖安全，Key 已轮换 |
| 代码可维护性 | main.py 4269 行 | 模块化拆分，最大文件 <1500 行 |
| 测试覆盖 | main.py 无测试 | 核心模块测试覆盖 >80% |
| WebSocket 延迟 | 串行发送 O(N) | 并行发送 O(1) |
| 首屏加载 | vis-network 500KB 同步加载 | 懒加载，首屏减少 500KB |
| 数据库性能 | N+1 查询 + 每次新建连接 | JOIN 查询 + 连接复用 |

---

## 实施顺序

1. **立即执行**: 轮换 API Key + 升级有漏洞依赖
2. **第一周**: 拆分 main.py + 消除代码重复
3. **第二周**: 补充测试 + 改善错误处理
4. **第三周**: WebSocket 优化 + 数据库优化
5. **第四周**: 前端优化 + Docker 镜像优化

---

## 风险评估

- **拆分 main.py**: 高风险，需要完整回归测试
- **依赖升级**: 中风险，可能引入兼容性问题
- **性能优化**: 低风险，主要是代码重构
- **Docker 优化**: 低风险，主要是配置调整

建议在独立分支进行所有修改，完成后再合并到主分支。